<!DOCTYPE html>
<html>
  <head>
    <%= favicon_link_tag '/5logo.png' %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>5 Roots</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "https://js.stripe.com/v2/", type: 'text/javascript' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= tag :meta, :name => "stripe-key", :content => STRIPE_PUBLIC_KEY %>

    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-136072502-2"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'UA-136072502-2');
    </script>


  </head>

  <body>
    <%= render 'shared/nav' %><br/><br/>

    <div class="container">
      <% flash.each do |name, msg| %>
        <% if msg.is_a?(String) %>
          <div class="alert alert-<%= flash_class_name(name) %>" role="alert">
            <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span>
            <span class="sr-only">Close</span>
            </button>
            <%= content_tag :div, msg, :id => "flash_#{name}" %>
          </div>
        <% end %>
      <% end %>
    </div>

  <% if current_user.is_a?(GuestUser) %>
    <script src="https://get.instach.at/chat-widget-1-0-0-js"></script>
      <script>
        var myLandbotPopup = new LandbotPopup({
        index: 'https://get.instach.at/5roots-form',
        launcherOptions: {
        image: "https://instach.at/whatsapp-icon/"
        }
        });
      </script>
      <script>
      // Show a proactive message after 4 seconds
      setTimeout(() => {
      myLandbotPopup.sendProactive("¡Hola! Estoy por aquí en caso que necesites ayuda.“);
      }, 4000);
      </script>
    <% end %>


    <%= yield %>
    <div class="container">
      <div class="form-group">
        <%= render "shared/footer" %>
      </div>
    </div>
  </body>
</html>
