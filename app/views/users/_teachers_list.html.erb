<div class="container" >
  <h1>Teachers</h1> Total <%= User.where(roles:"teacher").count %>

  <table class="table table-striped">
    <thead>
      <tr>
        <th colspan="3">Name</th>
      </tr>
    </thead>

    <tbody>
      <% @users.where(roles: :teacher).each do |user| %>
        <tr>
          <th scope="col"><%= link_to user.name, user  %></th>
        </tr>
      <% end %>
    </tbody>
  </table>
</div> 

<br>

<div class="container" >
  <h1>Admin</h1> Total <%= User.where(roles:"admin").count %>

  <table class="table table-striped">
    <thead>
      <tr>
        <th colspan="3">Name</th>
      </tr>
    </thead>

    <tbody>
      <% @users.where(roles: :admin).each do |user| %>
        <tr>
          <th scope="col"><%= link_to user.name, user  %></th>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>