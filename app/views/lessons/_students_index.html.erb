  <div class="container">

    <div class="p-3 mb-2 bg-light text-dark text-center">
      Clases próximas: <strong><%= @user.inverse_lessons.where( status_id: "Próxima").count(:time_duration_id) %></strong>  &nbsp &nbsp  &nbsp
      Clases completadas: <strong><%= @user.inverse_lessons.where( status_id: "Completada").count(:time_duration_id) %></strong> &nbsp &nbsp &nbsp
      Horas completadas: <strong><%= @user.inverse_lessons.where( status_id: "Completada").sum(:time_duration_id)/60 %></strong> hrs
    </div>

      <div class="p-3 mb-2 bg-light text-dark text-center">
        <p><strong>Política de cancelación</strong></p>
        Créditos ganados: <strong><%= @user.inverse_lessons.where( status_id: "Perdida por el profesor").count(:time_duration_id) %> </strong> &nbsp &nbsp
        Créditos perdidos: <strong><%= @user.inverse_lessons.where( status_id: "Perdida por el estudiante").count(:time_duration_id) %> </strong> &nbsp &nbsp
      </div>      <h1><%= t('lessons_title') %></h1>
  </div>

<table class="table">
  <thead>
    <tr>
      <th><%= t('type_of_class') %></th>
      <th><%= t('teacher_name_class') %></th>
      <th><%= t('incoming_date_class') %></th>
      <th><%= t('class_duration') %></th>
      <th><%= t('class_language') %></th>
      <th><%= t('status_class') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% for user in @lessons_students %>
      <tr>
        <td><%= user.class_type_id %></td>
        <td> <%= link_to User.find_by(id: "#{user.user_id}").name, "users/#{user.user_id}" %></td>
        <td><%= user.date_id.to_formatted_s(:long) %></td>
        <td><%= user.time_duration_id/60 %><p><%= t('hours') %></p></td>
        <td> <%= Language.find_by(id: "#{user.language_id}").name %></td>
        <td><%= user.status_id %></td>
        <td><%= mail_to "hello@5roots.co", t('report_class'), subject: t('report_subject') + " #{user.id} ", body: t('report_body_1') + " #{User.find_by(id: "#{user.student_id}").name}" + t('report_body_2') + " #{user.id} " + t('report_body_3'), class: "btn btn-outline-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

